const data = 'パタトクカシーー';
const first = data.slice(0, 1)
const second = data.slice(2, 3)
const third = data.slice(4, 5)
const fourth = data.slice(6, 7)

const concat = `${first}${second}${third}${fourth}`

console.log(concat)